# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# Transient workflow state — the per-workflow checklist is local working state.
.workflow/

# Engine artifacts — generated/dev only, never part of the repo. When an agent (Claude
# Code / Codex / OpenCode) is used to develop forge-ai it creates these; they are not
# relevant to end users and are regenerated on demand by template/sync.sh.
.claude/
.codex/
.opencode/
