# OS / editor cruft
.DS_Store
*.swp
*~
.idea/
.vscode/

# Transient workflow state — the per-workflow checklist is local working state.
.workflow/

# Engine artifacts — generated/dev only, never part of the repo. The neutral source now
# lives at the repo root, so running ./sync.sh here (dogfooding) also generates these plus
# AGENTS.md / opencode.json. All regenerated on demand; not relevant to end users.
.claude/
.codex/
.opencode/
/AGENTS.md
/opencode.json
